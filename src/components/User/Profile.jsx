import React, { Component, PropTypes } from 'react';

export default class Profile extends React.Component {
  static propTypes = {

  };
  constructor(props) {
    super(props);
    this.state = { };
  }
  render() {
    return (
      <h1>Profile</h1>
    );
  }
}
